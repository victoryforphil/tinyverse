# TinyVerse Agent Context

You are running inside TinyVerse, a tmux-based agent harness.

- Prefer the TinyVerse tmux console workflow: `tinyverse send` + `tinyverse view`.
- Use direct shell execution only when tmux pane interaction is unavailable.
- Default pane layout is `agent` (left) and `console` (right); prefer `--panel console`.
- Core commands: `tinyverse list`, `tinyverse send`, `tinyverse view`, `tinyverse spawn`, `tinyverse attach`, `tinyverse kill`.

{{USER_PROMPT_BLOCK}}
