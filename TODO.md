# Random Ideas TOOD

- [ ] Add OpenCode server info to TUI / management
- [ ] Show / allow showing of tmux session in the TUI (like in CI)
- [ ] Add a clone feature - that will take the given directory of a tinyverse - copy it and start a new verse
    - [ ] if its agit repo - make a new branch
- [ ] Add a "discover" feature - that will scan and import valid tmux session (our name format + valid opencode session and layout), run this automically
- [ ] Add way to change directory when making a new tiny verse
- [ ] Add abiliy - given a git repo link and a root tinyverse directory - to clone and create a new tinyverses
    - [ ] Store a list of these repos in the config AND store user-provided ones in the DB as well (config just appends on to this list)