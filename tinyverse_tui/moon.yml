$schema: "https://moonrepo.dev/schemas/project.json"

language: "rust"
stack: "backend"
type: "library"
dependsOn:
  - "tinyverse_lib"
  - "tinyverse_tui_components"

tasks:
  install:
    command: "cargo fetch"

  build:
    command: "cargo build -p tinyverse_tui"
    inputs:
      - "src/**/*"
      - "Cargo.toml"
      - "/Cargo.toml"
      - "/Cargo.lock"

  check:
    command: "cargo check -p tinyverse_tui"
    inputs:
      - "src/**/*"
      - "Cargo.toml"
      - "/Cargo.toml"
      - "/Cargo.lock"

  test:
    command: "sh"
    args: ["-c", "cargo nextest run -p tinyverse_tui || cargo test -p tinyverse_tui"]
    inputs:
      - "src/**/*"
      - "Cargo.toml"
      - "/Cargo.toml"
      - "/Cargo.lock"
